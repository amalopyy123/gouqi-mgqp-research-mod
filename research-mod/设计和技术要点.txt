如果要用户从大量的id中选一个，最好参考“自定义战斗”下面“按敌人选择”的逻辑，用户输入id，进入选择界面，界面中的选项有id和名字，有“加载上一批200项目”（开头附近），“有加载下一批200项”（结尾附近），重新输入id（开头和结尾），“返回“的选项可选；不要默认用户输入0而将“加载上一批200项目”禁用，要添加判断

"我发现物品比较多，能否参照“自定义战斗/角色图鉴”，支持上一批、下一批和重新输入起始ID"

如果一行输入太长的文字，使用mtool翻译会自动退出，经常是无报错的
一些防止字符串过长的方法：
- 物品名称：short_item_name(name, maximum = 15)
超过长度会截断为 前缀...。
- 战斗对白预览：battle_preview_text(text, maximum = 46)
超长对白会截断为 ...
- 角色图鉴详情：wrap_detail_lines
按固定 26 个字符换行。
- 地图详情：wrap_lines
按固定 42 个字符换行。

在mod上面输出的文字，会被mtool翻译，例如：ハーピーの羽会被翻译成哈比之羽

让用户输入数值时，要参考“修改当前人物好感度”，允许用户取消返回

实现某个功能，最好参考系统已有的实现或者界面，或者参考已经做过的实现或者界面

难以解决的问题，做log来判断

做csv之前，先确定column；生成csv的时候，让ai把问题记录下来（例如跟之前定的column的格式不兼容）

可用横线和省略号描述菜单层级

Focus -> Confirm

大的改动可以先定需求再做

让用户输入要考虑wrap-around navigation（循环导航），例如：
光标在第一行时按上键：跳到最后一行的对应列。
光标在最后一行时按下键：跳到第一行的对应列。




